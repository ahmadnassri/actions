name: pull-request-target

on: workflow_call

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}

jobs:
  auto-merge:
    secrets: inherit
    uses: ./.github/workflows/job-auto-merge.yml
    permissions:
      contents: write
      pull-requests: write
